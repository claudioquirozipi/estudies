---
import TopicLayout from '../../../layouts/TopicLayout.astro';
import RoadmapNode from '../../../components/RoadmapNode.astro';
import cssLogo from '../../../assets/logos/css.svg';

const color = '#5b9bd5';

const roadmap = [
  {
    title: 'Selectores',
    children: [
      { title: 'Selector universal', href: '/topics/css/selectores/selector-universal' },
      { title: 'Selector de tipo (elemento)', href: '/topics/css/selectores/selector-tipo' },
      { title: 'Selector de clase', href: '/topics/css/selectores/selector-clase' },
      { title: 'Selector de ID', href: '/topics/css/selectores/selector-id' },
      { title: 'Selector de atributo', href: '/topics/css/selectores/selector-atributo' },
      { title: 'Selectores combinadores', href: '/topics/css/selectores/selectores-combinadores' },
      { title: 'Pseudo-clases', href: '/topics/css/selectores/pseudo-clases' },
      { title: 'Pseudo-elementos', href: '/topics/css/selectores/pseudo-elementos' },
    ],
  },
];
---

<TopicLayout
  title="CSS"
  description="Flexbox, Grid, animaciones, variables CSS, arquitectura de estilos y las novedades del lenguaje."
  color={color}
  topic="CSS"
  slug="css"
  logo={cssLogo}
>
  <div class="roadmap">
    {
      roadmap.map((category) => (
        <div class="roadmap-card" style={`--color: ${color}`}>
          <div class="roadmap-card-accent" />
          <h3 class="roadmap-card-title">{category.title}</h3>
          <div class="roadmap-card-body">
            {category.children?.map((child) => <RoadmapNode node={child} />)}
          </div>
        </div>
      ))
    }
  </div>
</TopicLayout>

<style>
  .roadmap {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .roadmap-card {
    position: relative;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.25rem 1.4rem 1.4rem;
    overflow: hidden;
  }

  .roadmap-card-accent {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--color);
    opacity: 0.75;
  }

  .roadmap-card-title {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 0.75rem;
  }

  .roadmap-card-body {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }
</style>
